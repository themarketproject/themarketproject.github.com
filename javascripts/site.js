jQuery(document).ready(function($) {
  var parallax = $('#parallax-background');  
  // window.onscroll = function(e) {  // note: most browsers presently use prefixes: webkitTransform, mozTransform etc.  
  //   parallax.css('transform', 'translate3d(0px, -' + ((window.scrollY)/8) + 'px, 0px)');
  // }
});